TO DO LIST FOR TA_HELPER
=====================================================================

X - Need to add multiple email addresses per student functionality.

- Remove all configurations from source, so updates can carry
over more easily, aren't all mangled in different directories. This
will probably be best done with an input or configuration file of
some sort.

- GUI (longer term goal)
